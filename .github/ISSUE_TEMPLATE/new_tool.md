---
name: New tool
about: Propose a new tool for this repo (so two people don't build the same thing)
title: "[tool] "
labels: new tool
---

**Directory name** (lowercase-with-dashes — this is what it'll be called):

**What it does**, in a sentence or two:

**Stack** (language, framework, runtime):

**What it needs from the slab API** (which endpoints, roughly):

**Does it already exist as its own repo?** If so, link it — we can bring it in with `git subtree`
so your history comes along.

**Maintainer** (your handle):
